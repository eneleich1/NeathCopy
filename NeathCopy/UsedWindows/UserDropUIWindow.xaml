<Window x:Class="NeathCopy.UsedWindows.UserDropUIWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NeathCopy.UsedWindows" Closing="Window_Closing"
        mc:Ignorable="d" SizeToContent="WidthAndHeight" Topmost="True"  WindowStartupLocation="CenterOwner"
        Title="UserDropUIWindow"  Style="{DynamicResource UsedWindowStyle}" Icon="/NeathCopy;component/Themes/Images/Windows8Images/info.png">
    <Grid Background="{DynamicResource ControlBackgroundBrush}">
        
        <Grid Margin="20"  HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="1" Orientation="Horizontal" >
                <ComboBox FontWeight="Bold" VerticalAlignment="Center" Width="80" SelectedIndex="{Binding SelectedOperationIndex}">
                    <ComboBoxItem Content="{DynamicResource t55}"/>
                    <ComboBoxItem Content="{DynamicResource t56}"/>
                </ComboBox>
                <TextBox Width="200" Margin="3,0,3,0" Foreground="{DynamicResource TextBrush}" Background="{DynamicResource ControlBackgroundBrush}"
                         Text="{Binding Destiny, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Background="Transparent" FontWeight="Bold" Command="{Binding BrowseCommand}"  Margin="5,0,0,0" Width="25"   Content="..."/>
            </StackPanel>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0" Height="23">
                <Button Background="Transparent" Foreground="Blue" FontWeight="Bold" Content="{DynamicResource t57}" Command="{Binding OkCommand}" Width="60" IsDefault="True"/>
                <Button Background="Transparent" FontWeight="Bold" Content="{DynamicResource t58}" Command="{Binding CancelCommand}" Margin="20,0,0,0" Foreground="Red" Width="60" IsCancel="True"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
